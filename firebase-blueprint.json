{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile information",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "role": { "type": "string", "enum": ["user", "admin"] }
      },
      "required": ["uid", "email"]
    },
    "QuizProject": {
      "title": "QuizProject",
      "description": "A quiz project created by a user",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "name": { "type": "string" },
        "createdAt": { "type": "number" },
        "steps": { "type": "array" }
      },
      "required": ["id", "userId", "name", "createdAt", "steps"]
    },
    "ProjectImage": {
      "title": "ProjectImage",
      "description": "An image associated with a quiz project option",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "data": { "type": "string" }
      },
      "required": ["id", "data"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profile documents"
    },
    "/users/{userId}/projects/{projectId}": {
      "schema": "QuizProject",
      "description": "Quiz projects created by users"
    },
    "/users/{userId}/projects/{projectId}/images/{imageId}": {
      "schema": "ProjectImage",
      "description": "Images for project options"
    }
  }
}
